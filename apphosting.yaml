# Firebase App Hosting Configuration for Tamil Nadu Startup Connect
# Secrets must NOT be committed to Git. Configure secrets in Firebase Console / Cloud Secret Manager.

runConfig:
  minInstances: 0
  maxInstances: 10
  concurrency: 80
  cpu: 1
  memoryMiB: 1024

env:
  - variable: NODE_ENV
    value: "production"
